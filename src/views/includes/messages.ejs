<% if (errors.length > 0) { %>
<div class="row">
  <div class="alert alert-danger"><% errors.forEach(element => { %>
    <%= element %><br>
    <% }); %> </div>
</div>
<% } %>

<% if (success.length > 0) { %>
<div class="row">
  <div class="alert alert-success"><%= success %></div>
</div>
<% } %>